type Mutation {
    applyRoute(route: String!, month: String!, partitionKey: String!): applyRouteResponse
}

type applyRouteResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
}
